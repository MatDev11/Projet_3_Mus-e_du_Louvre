{% extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Acceiul{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-lg-12">
            <h1>Billettrie</h1>
        </div>
    </div>
    {% for message in app.session.flashbag.get('success') %}
        <br/>
        <div class="flashbag">
            <span>{{ message }}</span>
        </div>
    {% endfor %}

    <div class="row center">


        {{ form_start(form, {'attr': {'class': 'col-lg-6'}}) }}
        <h3>Choisir une date :</h3>

        {{ form_errors(form) }}
        <br>
        <div id="datepicker"></div>
        <br>

        {{ form_row(form.date_commande) }}

        <h3>Choisir le nombre de billet :</h3>
        <br>

        {{ form_row(form.qte_place) }}
        {{ form_row(form.type_tarif) }}

        {{ form_widget(form.Suivant, {'attr': {'class': 'btn btn-primary '}}) }}


        {{ form_end(form) }}


    </div>

{% endblock %}